
services:
  api:
    build: ./api
    container_name: api_c
    ports:
      - "4000:4000"
    volumes:
      - ./api:/app
      - /api/node_modules
  developer-group:
    build: ./developer-group
    container_name: developer_c
    ports:
      - "5173:5173"
    stdin_open: true
    tty: true